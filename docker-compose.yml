services:
  mybizpro-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_KEY: ${API_KEY:-}
        VITE_API_KEY: ${VITE_API_KEY:-}
    image: mybizpro-app:latest
    expose:
      - "80"
    restart: unless-stopped
    environment:
      NODE_ENV: production
